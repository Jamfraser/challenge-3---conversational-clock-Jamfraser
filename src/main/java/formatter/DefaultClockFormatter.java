package formatter;

import com.time.ConversationTime;
public class DefaultClockFormatter implements ClockFormatter{

    @Override
    public String format(ConversationTime conversationTime) {
        // Implement log to format the conversationTime into a conversational phrase
        // Example: return "It's one o'clock"

        // temporary return statement
        return "It's placeholder o'clock";
    }
}
